export type Feature = {
  imgUrl: string;
  title: string;
  subtitle: string;
};

export type ExplorerImage = {
  id: string;
  imgUrl: string;
  title: string;
};

export type SocialMedia = {
  name: string;
  url: string;
};
