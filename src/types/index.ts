export type Feature = {
  imgUrl: string;
  title: string;
  subtitle: string;
  width?: number;
  height?: number;
};

export type ExplorerImage = {
  id: string;
  imgUrl: string;
  title: string;
  width: number;
  height: number;
};

export type SocialMedia = {
  name: string;
  url: string;
};
